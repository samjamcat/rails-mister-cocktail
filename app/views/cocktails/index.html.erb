<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3"><h1>Cocktails</h1>

    <%= link_to 'Add Cocktail', new_cocktail_path, class: "btn btn-primary"%>



      <% @cocktails.each do |cocktail| %>
        <h2><%= cocktail.name %></h2>
        <p>***</p>


      <%= link_to "Edit", edit_cocktail_path(:name), class: "btn btn-primary" %>
      <%= link_to "Read more", cocktail_path(@cocktail), class: "btn btn-primary"  %>

      <% end %>
    </div>
  </div>
</div>
